# Day 2 — Auth + RBAC (2026-06-05)

## Done
- Signed httpOnly session cookie (`sentinel_session`) with HMAC payload + TTL
- `POST /api/v1/auth/login`, `POST /api/v1/auth/logout`, `GET /api/v1/auth/me`
- RBAC middleware on ingest, search, retrieval, and admin stub routes
- Client module: `GET /api/v1/clients/me`, `PATCH /api/v1/clients/me/onboarding` (stub)
- Audit events on login, logout, onboarding update (hash chain)
- `pnpm --filter @biovault/sentinel-api db:seed` demo users
- OpenAPI updated; Vitest integration tests for auth/RBAC

## Review
- [ ] `pnpm install && pnpm turbo test`
- [ ] `pnpm --filter @biovault/sentinel-api db:seed` then login curl
- [ ] Client cannot hit `/api/v1/admin/*`; viewer cannot POST ingest

## Next (Day 3)
- Ingest intake; per-file SHA-256; `files` collection
