# Days 5–6 — Ingest verify/seal + client search (2026-06-07)

## Done

### Day 5
- **Read-back verify:** `processTapeVerify` SHA-256 via `TapeLibraryAdapter`; files → `on_tape`; job → `sealed`
- **Failure handling:** checksum mismatch → `failed` job, tape unblocked from `writing`, staging retained, audit `ingest.job_failed`
- **Tape fill:** cumulative `fillPercent` per write (not overwrite)
- **Ingest report:** `GET /api/v1/ingest/jobs/{jobId}/report` (sealed only; no tape location)
- **RBAC:** ingest read (job status + report) open to all client roles; upload remains `client_admin`
- **Tests:** verify/seal pipeline, report 404/200, checksum mismatch, no location leak

### Day 6
- **Search API:** `GET /api/v1/search/files` — tenant-scoped, `on_tape` only
- **Filters:** `q` (filename + keyword HMAC tokens), `from`/`to`, `fileType`, `category`, `limit`/`offset`
- **Security:** client response never includes barcode/rack/slot/checksum; requires `requireClientUser`
- **OpenAPI:** `FileSearchResponse`, full query params
- **Tests:** 8 integration cases + unit service test

## Review
- [x] `pnpm turbo test` (39 sentinel-api tests)
- [x] `pnpm turbo typecheck --filter=@biovault/sentinel-api`
- [x] No file blobs in Mongo — metadata + staging only
- [x] Client APIs omit tape barcode/rack/slot

## Next (Day 7)
- Retrieval queue, 60s unassigned alert, 15m SLA timer
