# Day 5 — Read-back verify, sealed jobs, ingest report (2026-06-07)

## Done
- **D5:** `processTapeVerify` — SHA-256 read-back via `TapeLibraryAdapter`; files → `on_tape`; job → `sealed`
- BullMQ worker chains write + verify in one job
- Ingest staging purged after successful verify (`STAGING_PATH/ingest/{jobId}/`)
- Audit `ingest.files_indexed` stores `fileIds` + `checksums` only (no raw filenames)
- `keywordSearchTokens` + HMAC index (mirrors filename tokens)
- `GET /api/v1/ingest/jobs/{jobId}/report` — client confirmation report (sealed only)
- OpenAPI: `IngestReport`, `IngestReportResponse`
- `SEARCH_TOKEN_SECRET` in `deploy/.env.example`

## Review
- [x] `pnpm turbo test --filter=@biovault/sentinel-api` (27 tests)
- [x] `pnpm turbo typecheck --filter=@biovault/sentinel-api`
- [ ] Local curl: upload → poll `sealed` → fetch report

## Next (Day 6)
- Client search API (filename tokens, date, type, category — no rack/slot)
