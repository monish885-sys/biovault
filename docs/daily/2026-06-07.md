# Days 3–4 — Ingest intake + tape write (2026-06-07)

## Done
- **D3:** `POST /api/v1/ingest/jobs` multipart upload; stream to `STAGING_PATH`; per-file SHA-256; `files` + `ingest_jobs` collections; audit events; client-visible job response (no tape location)
- **D4:** BullMQ `ingest` queue + worker; `processTapeWrite` via `TapeLibraryAdapter`; `file_locations` with barcode/block/offset; tape catalog seeded (`TAPE-ACME-001/002`); job advances to `verifying` (read-back verify deferred to D5)
- OpenAPI schemas: `IngestJobSummary`, `IngestFileSummary`
- Vitest integration tests for intake checksums and tape write

## Review
- [x] `pnpm turbo test`
- [ ] `pnpm --filter @biovault/sentinel-api db:seed` then multipart curl ingest
- [ ] No file blobs in Mongo — metadata + staging only
- [ ] Client ingest response omits barcode/rack/slot

## Next (Day 5)
- Read-back SHA-256 verify; job `sealed`; ingest report API
