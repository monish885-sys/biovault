# Days 7–8 — Retrieval jobs + admin ops queue (2026-06-08)

## Done

### Day 7
- **Retrieval API:** `POST /api/v1/retrieval/jobs` creates tenant-scoped job from `fileId` (`on_tape` only)
- **SLA:** `dueAt = createdAt + 15 minutes`; response includes `slaRemainingSeconds` and `slaOverdue`
- **60s alert:** BullMQ delayed `unassigned-alert` job; worker logs + audit `retrieval.unassigned_alert` when still pending/unassigned
- **Client tracker:** `GET /api/v1/retrieval/jobs` and `GET /api/v1/retrieval/jobs/{jobId}` (no tape location)
- **Audit:** `retrieval.job_requested` on create
- **Security:** `requireClientUser` + RBAC; duplicate active retrieval rejected

### Day 8
- **Admin queue:** `GET /api/v1/admin/jobs` — internal roles only
- **SLA countdown:** `slaRemainingSeconds` / `slaOverdue` on every job row
- **Technician view data:** tape barcode, rack, slot via `file_locations` + `tapes`; client name; requester email
- **Filters:** `status`, `overdue`, `limit`, `offset`
- **OpenAPI:** full request/response schemas for retrieval + admin jobs

## Review
- [x] `pnpm turbo test`
- [x] `pnpm turbo typecheck --filter=@biovault/sentinel-api`
- [x] No file blobs in Mongo — metadata + job orchestration only
- [x] Client APIs omit tape barcode/rack/slot

## Next (Day 9)
- Staging copy from tape; signed download URL; purge after TTL/download
