# Days 11–13 — Audit, certificates, client + admin portals (2026-06-09)

## Done

### Day 11
- **Audit export:** `GET /api/v1/admin/audit/events` (paginated), `GET /api/v1/admin/audit/export` (hash chain verification)
- **Client compliance export:** `GET /api/v1/audit/export` (tenant-scoped for `client_admin` / `compliance_officer`)
- **Ingest certificates:** signed PDF via `pdf-lib` + Ed25519/RSA (`CERT_SIGNING_KEY_PATH` or dev key in `STAGING_PATH`)
- **Routes:** `POST/GET /api/v1/ingest/jobs/{jobId}/certificate`, `GET .../certificate/download`
- **Auto-issue** on ingest job seal; PDF hash stored in `certificates` collection

### Day 12
- **Client portal:** login, metadata search, request retrieval, live job tracker with SLA countdown, download when ready
- Vite dev proxy for `/api`; production build via `pnpm --filter @biovault/client-portal build`

### Day 13
- **Admin portal:** login, live retrieval queue (5s poll), SLA timers, assign → start → complete workflow, tape barcode/rack/slot, tape inventory tab
- Vite dev proxy; production build via `pnpm --filter @biovault/admin-portal build`

## Review
- [x] `pnpm turbo test typecheck build`
- [x] No file blobs in Mongo — certificates on disk under `STAGING_PATH/certificates/`
- [x] Client APIs omit tape location; admin shows tape detail
- [x] OpenAPI updated for audit + certificate endpoints

## Next (Day 14)
- E2E demo script, runbooks, tag `v0.1.0-mvp`
