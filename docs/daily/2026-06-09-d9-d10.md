# Days 9–10 — Staging download + tape inventory (2026-06-09)

## Done

### Day 9
- **Staging copy:** `completeRetrievalJob` reads from tape via `TapeLibraryAdapter`, writes to `STAGING_PATH/retrieval/{jobId}/`
- **Admin workflow:** `PATCH /api/v1/admin/jobs/{jobId}` (assign → in_progress); `POST .../complete` stages file and marks `ready`
- **Signed download:** HMAC-signed token (`DOWNLOAD_TOKEN_SECRET`); `GET /api/v1/retrieval/download?token=...` (no cookie auth)
- **Client tracker:** `downloadUrl` + `downloadExpiresAt` on job summary when `ready`
- **Purge:** staging removed after successful download; BullMQ `download-expiry` job purges on TTL
- **Audit:** `retrieval.staged`, `retrieval.downloaded`, `retrieval.staging_purged`, `retrieval.expired`

### Day 10
- **Tape inventory:** `GET /api/v1/admin/tapes` — barcode, rack, slot, status, fill %, write cycles, age
- **Health scores:** `computeTapeHealthScore` — green / amber / red from write cycles, age, fill %
- **Ingest hook:** tape health recomputed on each write cycle increment

## Review
- [x] `pnpm turbo test`
- [x] No file blobs in Mongo — staging on disk only
- [x] Client APIs omit tape barcode/rack/slot
- [x] Download link time-limited and purged after use

## Next (Day 11)
- Immutable `audit_events` export; signed ingest PDF certificate
